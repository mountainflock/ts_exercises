// Ask the user to enter a number.
// Then, display a formula of that number being multiplied by 2 and the result of multiplying it by 2.

const n = parseInt(prompt("Enter a number:")!);
console.log(`${n} * 2 = ${n * 2}`);
