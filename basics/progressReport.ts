// Create two variables. Assign your name to the first one.
// Assign the total time you've spent studying programming in minutes to the second one.
// Then, display two messages on screen. The first message should use the first variable in its text.
// The second message should use the second variable, respectively.

const name = "Marina";
const minutes = 210;
console.log(`My name is ${name}.`);
console.log(`I've been studying programming for ${minutes} minutes.`);
