// Ask the user to enter their weight in kilograms.
// Then tell them what their weight would be on Mars.

const weight = parseInt(prompt("Enter your weight in kg:")!);
console.log(`You'd weigh ${weight * 0.38} kg on Mars!`);
